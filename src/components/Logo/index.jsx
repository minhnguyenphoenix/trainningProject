// import PropTypes from 'prop-types';
// import { Avatar, Space } from 'antd';

// function LogoIcon({ urlImg, size, shape }) {
//   return <Avatar { ...urlImg ?  { src={urlImg} : icon= {icon} }} size={size} shape={shape} />;
// }

// LogoIcon.defaultProps = {
//   urlImg: '',
//   size: 64,
// };

// LogoIcon.propTypes = {
//   urlImg: PropTypes.string,
//   size: PropTypes.number,
//   shape: PropTypes.shape.isRequired,
// };

// export default LogoIcon;
